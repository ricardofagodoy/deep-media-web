export type Optimization = {
    id: string,
    connector: string,
    date: string,
    campaign: string,
    campaign_id: string,
    target: number,
    margin: number,
    ad_cost: number,
    optimize: string,
    before: number,
    after: number
}