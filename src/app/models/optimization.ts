export type Optimization = {
    id: string,
    connector: string,
    date: string,
    campaign: string,
    target: number,
    margin: number,
    optimize: string,
    before: number,
    after: number
}